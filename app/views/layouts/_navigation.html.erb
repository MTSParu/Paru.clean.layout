<nav class="navbar navbar-expand-lg nav-bg-color">
<div class="container-fluid">
  <%= link_to "ALFA BLOG", root_path, class:"navbar-brand", id: "logo" %>
  <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
      <li class="nav-item">
        <%= link_to "Bloggers", users_path, class: "nav-link" %>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle text-white" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
          Articles
        </a>
        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
        <% if logged_in? %>
          <%= link_to "Creat a new article", new_article_path, class: "dropdown-itom text-secondary" %>
          <% end %> <br>
          <%= link_to "View article", articles_path, class: "dropdown-itom text-secondary" %>
          <li><hr class="dropdown-divider"></li>
          <li><a class="dropdown-item" href="#">Something else here</a></li>  
        </ul>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle text-white" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
          Categories
        </a>
        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
        <% if logged_in? && current_user.admin? %>
          <%= link_to "Creat a new category", new_category_path, class: "dropdown-itom text-secondary" %>
          <% end %> <br>
          <%= link_to "View Categories", categories_path, class: "dropdown-itom text-secondary" %>
        </ul>
      </li>
        <% if logged_in? %>
        <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle text-white" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
          <%= "(admin)" if current_user.admin? %> Profile [<%= current_user.username %>]
        </a>
        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
          <%= link_to "View your profile", user_path(current_user), class: "dropdown-itom text-secondary" %> <br>
          <%= link_to "Edir your profile", edit_user_path(current_user), class: "dropdown-itom text-secondary" %> <br>
          <%= button_to "Delete profile", user_path(current_user), class: "dropdown_item text-danger", id: "profile-delete-btn", method: :delete %>
        </ul>
      </li>
        <li class="nav-item">
      <%= link_to 'logout', logout_path, class: "nav-link" %>
        </li>
        <% else %>
        <li class="nav-item">
      <%= link_to 'log in', login_path, class: "nav-link  " %>
        </li>
        <li class="nav-item">
      <%= link_to 'Sign up', signup_path, class: "nav-link" %>
        </li>
        <% end %>
    </ul>
    <form class="d-flex" role="search">
      <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
      <button class="btn btn-outline-success" type="submit">Search</button>
    </form>
  </div>
</div>
</nav>
